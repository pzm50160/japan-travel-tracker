# 旅遊記帳 — 專案交接說明

> 更新於 2026-08-30（第五版）。新對話請先讀這份，可略過先前的討論過程。

## 專案基本資料

| 項目 | 內容 |
|---|---|
| 程式 | 單一檔案 `index.html`（約 5,100 行，HTML+CSS+JS 全在一起）|
| 線上網址 | https://pzm50160.github.io/japan-travel-tracker/ |
| 部署方式 | GitHub Pages，推上 main 分支約 30 秒後生效 |
| 發布指令 | 雙擊 `發布上線.bat`（git add → commit → push）|
| 資料庫 | Supabase 專案 `lkojnzprqqdmqhzzlqtb`（免費方案）|
| 後台 | https://supabase.com/dashboard/project/lkojnzprqqdmqhzzlqtb |
| 使用者規模 | 18 個旅程、21 位成員、6 個註冊帳號 |
| 實際使用中 | 九州之旅（8/18~8/27），成員：擁有者本人、小鳳 |

功能分頁：首頁、紀錄、掃描、分攤、統計、行李、牌局計分、設定。
（回憶錄分頁已隱藏，見「已隱藏／未完成的功能」）

---

## 目前狀態

**全部已上線且驗證過**（2026-08-19 查核：本機無未提交改動、線上含所有功能、Edge Function 回應正常）。

| 項目 | 狀態 |
|---|---|
| RLS 資安修補（rls / functions / guest 三支 SQL）| ✅ 已執行並實測 |
| 匿名登入開關 | ✅ 已開啟 |
| 免註冊不得建立旅程 | ✅ 已執行 `supabase_guest_no_create.sql` |
| 掃描改走後端代理（Edge Function）| ✅ 已部署並運作中 |
| 收據品項歸屬功能 | ✅ 已上線 |
| 同一天內排序（`sort_order`）| ✅ 欄位已建立、前端已上線 |
| 排序改成拖曳 + 時間欄位分離 | ⚠️ **程式已改好，但 `supabase_time_field.sql` 還沒執行**。SQL 沒跑就發布前端，記帳會存不進去（欄位不存在）|
| Edge Function 讀收據時間 | ❌ **確定沒有重新部署**（見 5-3，掃描存進去的時間是「掃描當下」不是收據上的）|
| 效能量測碼 | ⚠️ **仍在線上**，是暫時的除錯介面，見下方 |

---

## 一、資安修補（已完成）

**原始漏洞**：`trips`／`members`／`expenses` 各有一條 `public read write` 政策
（對象 `{public}`、條件 `true`、動作 `ALL`），任何人拿公開金鑰（就寫在 index.html 裡）
都能讀取、修改、刪除全部資料。`user_trips` 的政策也未限制 `trip_id`。

**修法**：移除那四條政策，改成「必須是該旅程成員」，並新增三個 `SECURITY DEFINER`
函式讓邀請碼流程在鎖上後仍能運作。

**驗證**（匿名金鑰實測）：讀取三張表從 18/21/38 筆變成全部 0 筆；新增旅程回 401；
呼叫邀請碼函式回 42501 permission denied。

相關檔案：`supabase_rls.sql`、`supabase_functions.sql`。

---

## 二、免註冊模式（已完成）

原本的「訪客模式」沒有任何身分，資料庫無從判斷誰能看哪個旅程。
改用 **Supabase 匿名登入**：使用者不填任何資料，但系統發給一個 `auth.uid()`，RLS 才能運作。

**權限設計**：可以「參與」——加入旅程、記帳、看分攤；不能「破壞」——建立旅程、
改旅程設定、移除其他成員。

| 層級 | 實作 |
|---|---|
| 資料庫 | `supabase_guest.sql`：`is_anon_user()` 函式 + 限制 trips 的 UPDATE、members 的 DELETE |
| 資料庫 | `supabase_guest_no_create.sql`：限制 trips 的 INSERT |
| 程式 | `isGuestUser()` 判斷 `state.user.is_anonymous` |
| 程式 | `renderMyTrips()` 隱藏「建立新旅程」、標籤顯示「免註冊」 |
| 程式 | `showCreateTrip()` 防呆，繞過按鈕也擋得住 |

**為什麼要擋建立旅程**：匿名身分綁在該裝置瀏覽器，清除資料就找不回來，
它建立的旅程會變成無主旅程——專案裡現存三個無主旅程，成因很可能就是這個。

---

## 三、收據掃描（本次大改，重點）

### 3-1 架構：改走後端代理

**以前**：前端直接呼叫 Anthropic API，金鑰存在每支手機的 localStorage。
問題是每位旅伴都要自己申請金鑰，而且金鑰放瀏覽器可被竊取。

**現在**：前端呼叫 Supabase Edge Function `scan-receipt`，由它代呼叫 Anthropic。

```
手機 → Supabase Edge Function（金鑰在這）→ Anthropic API
```

| 項目 | 內容 |
|---|---|
| 函式原始碼 | `supabase/functions/scan-receipt/index.ts`（193 行）|
| 金鑰位置 | Supabase → Edge Functions → Secrets → `ANTHROPIC_API_KEY` |
| 部署方式 | Dashboard → Edge Functions → Functions → Open Editor → 貼上 → Deploy |
| 存取控制 | 必須已登入**且至少屬於一個旅程**（含免註冊者）|
| 提示詞位置 | **在函式裡**，不在 index.html |
| 圖片上限 | 伺服器端擋 6MB |

⚠️ **改提示詞或模型參數要重新部署函式**，不是跑 `發布上線.bat`。
這是為了不讓別人拿你的金鑰當通用 AI 用而付出的代價。

⚠️ **設定頁的 API Key 欄位已隱藏**（用 HTML 註解包起來）。旅伴不必再各自申請金鑰。

### 3-2 送圖前先縮圖

`toBase64ForScan()`：長邊縮到 **1568px**、JPEG 品質 **0.72**。

- 為什麼要縮：iPhone 原圖約 8MB，轉 base64 膨脹 1.33 倍 → 超過 API 的 10MB 上限直接失敗
- 為什麼是 1568px：模型本身就會把圖片縮到這個邊長，先縮不損失辨識精度
- 為什麼品質 0.72：0.85 會產生 1.1MB，實測降到 0.72 只剩 330KB（少 70%）
- 存進 Storage 的也是這張縮圖（原本傳原圖，上傳要 7~17 秒）
- 縮圖失敗會自動退回原本做法，不會整個掃不了

### 3-3 品項歸屬（誰的東西）

掃描結果的每個品項可以點頭像設定，**預設是「這項是我的、我自己付」**。

| 選項 | 存檔後 |
|---|---|
| 👥 大家平分 | 付款人=我，分攤=全員 |
| 🙂 某成員（未勾已收款）| 付款人=我，分攤=只有他 → **他欠我** |
| 🙂 某成員（勾了已收款）| 付款人=他，分攤=只有他 → **餘額不變**（他當場給了現金，等於他自己付）|
| 🙂 我（預設）| 付款人=我，分攤=只有我 |
| 🚫 不列入這筆帳 | 完全排除，**連同該項應攤的稅金一起從合計扣掉** |

**數量大於 1 可以只套用到其中幾個**，程式會自動把該行拆成兩列
（例如「巧克力 ¥278×2」可以 1 個留給自己、1 個排除或給別人）。

實測驗證（7-ELEVEN ¥1,558 收據）：

| 情境 | 結果 |
|---|---|
| A 那項未收款 | 拆 2 筆，A 欠我 ¥127（含稅比例分攤，非 ¥118）|
| A 已給錢 | 拆 2 筆，雙方餘額 0 |
| 全部標平分 | 合 1 筆 ¥1,558，A 欠我 ¥779 |
| 排除 ¥278×1 | 記錄 ¥1,258（扣掉該項與其稅金）|

⚠️ **預設行為變了**：以前掃描存檔一律平分，現在預設全算自己的。
**共食收據（一起吃飯）要記得手動標成「大家平分」**，否則旅伴不用分攤。
若用久覺得不順手，改回預設平分只要動 `showReceiptResult()` 裡的一行。

⚠️ **存檔後不能重新分配品項**。品項明細沒有結構化存進資料庫，只留在備註文字裡。
存檔後只能編輯每筆帳的金額／付款人／分攤方式。標錯要重來就是刪掉重掃。

### 3-4 日期防呆與時間排序

**日期防呆（前端，已上線）**：AI 曾把交易編號、電話誤讀成日期，實際發生過
`2021-08-24`、`2014-08-18`，那些帳目會被歸到錯的年份、排在列表最下面。
現在辨識出的日期若離今天超過一年（或格式錯、讀不到），自動改成今天並在結果卡片顯示黃色提示。

**時間排序（需要函式重新部署才會生效）**：提示詞新增
- `time` 欄位：讀收據上印的交易時間（HH:MM）
- 日期規則：只取收據上明確印出的購買日期，不要從交易編號、電話、商品編號推測

前端把 `time` 轉成當天分鐘數存進 `sort_order`（見第五章）。

### 3-5 目前的模型參數（在 Edge Function 裡）

```typescript
model: "claude-sonnet-4-6"
thinking: { type: "disabled" }
output_config: { effort: ??? }   // 檔案裡是 "high"，但線上部署的版本未確認
max_tokens: 3000
```

**待確認**：`effort` 從 `low` 改成 `high` 是為了比較速度，但沒有回報是否已部署、
也沒有比較數據。另外「讀收據時間 + 日期規則」的提示詞更新也需要重新部署才生效。
新對話可先確認線上函式的實際內容。

三個參數都明寫、不依賴預設值，因為**不同世代的預設不同**：
不帶 `thinking` 時，`sonnet-4-6` 是不思考，`sonnet-5` 卻會自動思考。

---

## 四、效能量測（暫時的，記得移除）

掃描頁最上方有一個**橘色框**顯示秒數，程式裡都標了「【暫時】」：

```
⏱ 辨識：縮圖 0.5 秒 ・ AI 辨識 14.6 秒
送出圖片 330KB ・ 辨識出 5 個品項
💾 儲存：上傳照片 6.9 秒 ・ 寫入資料 0.6 秒 ・ 重載全部 0.4 秒
```

- 相關函式：`showTiming()` / `renderTiming()` / `clearTiming()`，資料存在 localStorage 的 `jpTiming`
- 目前刻意保留：可以當作「有沒有更新到最新版」的判斷依據（看得到橘框＝新版）
- **所有使用者都看得到**，包括旅伴。確認大家都更新後應該移除

### 已量到的數據（供參考）

| 次數 | 圖片 | AI 辨識 | 上傳照片 |
|---|---|---|---|
| 1 | 1178KB | 14.6s | 3.7s |
| 2 | 1092KB | 30.9s | 17.5s |
| 3 | 330KB | 17.1s | 6.9s |

**結論**：同一份程式、圖片小三倍，辨識時間沒有等比變快，波動來自網路與伺服器負載
（實測上行速度在 0.4~2.5Mbps 之間跳動）。速度問題主要不在程式。

---

## 五、紀錄頁（本次新增）

### 5-1 列表精簡

| 內容 | 顯示方式 |
|---|---|
| 掃描的收據照片 | 標籤列的 **📷 收據**，點了看大圖（不在列表展開）|
| 自己拍的照片 | 64px 縮圖，直接看得到 |
| 品項明細 | 不在列表顯示，點進去看 |
| 每人分攤 | 標籤下方一排小膠囊：`🙂 名 NT$421  🙂 小鳳 NT$421`（用 `getShares()` 算，與分攤頁一致）|
| 時間 | 有 `sort_order` 時顯示 `🕐 12:38` |

**怎麼分辨收據與自拍照片**：掃描存檔一定會把品項明細寫進備註（含「¥」），
手動記帳幾乎不會，所以用 `/¥/.test(e.notes)` 判斷。
**限制**：手動記帳時若在備註打了「¥」會被誤判成收據。要百分之百準確得加欄位標示來源，
評估後認為不值得。

### 5-2 搜尋

分頁按鈕上方有搜尋框，即時過濾，右側 ✕ 清除。
範圍：名稱、店名、**備註（品項明細在裡面）**、分類、付款方式、日期、金額。
搜尋時上方總計只算符合的筆數並標示「（共 N 筆中符合）」。三個分頁都適用。

### 5-3 同一天內的排序（拖曳）與時間欄位

**2026-08-30 大改**：原本一個 `sort_order` 同時當「排序位置」和「顯示的 🕐 時間」用，
拆成兩個欄位（`supabase_time_field.sql`，**尚未執行**）。

| 欄位 | 用途 | 誰會寫 |
|---|---|---|
| `sort_order`（double）| 純粹是排序位置，不是時間 | 掃描、手動記帳、拖曳 |
| `time_min`（smallint，可 null）| **真實時間**（當天分鐘數）。只有收據上印的、或使用者自己填的才有值 | 掃描（讀得到才給）、編輯視窗的「時間」欄位 |
| `sort_manual`（boolean）| 位置是手動拖出來的，列表標一個 **↕ 手動** | 拖曳 |

**為什麼要拆**：使用者反映列表上出現 `🕐 01:50` 這種不可能的用餐時間。
原因是 Edge Function 沒重新部署、回傳的資料沒有 `time`，程式就退回「掃描當下的時間」，
凌晨補掃的整批收據就全變 01:50。現在讀不到收據時間 → `time_min` 留 null → **不顯示時間**
（`sort_order` 照樣給值，只是純排序用，不會被當成時間顯示出來）。

**排序規則**：日期新到舊；同一天內依 `sort_order` 由早到晚；沒有值的排最後。
先照時間排，**手動拖過的以拖曳的位置為最終定案**（拖曳只動位置，不會動到 `time_min`）。

**自己設定時間**：編輯視窗的「日期」旁邊多了「時間（選填）」。
填了 → 寫進 `time_min`，同時把 `sort_order` 設成同一個值、清掉 `sort_manual`（回到照時間排）；
清空 → 該筆不顯示時間，順序維持不變。新增花費時預設帶入當下時間，可以改或清掉。

**拖曳操作**：日期標題列右側的 **⇅ 排序** 按鈕（該日超過一筆才出現，只在「按日期」分頁）。
進入後**按住那一列的任何地方**上下拖（左邊的 ☰ 只是提示），拖到畫面上下緣會自動捲動，
放開才寫資料庫。按「完成排序」離開。

- 用 pointer 事件（手機觸控、電腦滑鼠同一套）
- **一開始只有 ☰ 那一小塊可拖（約 27×35），手指按偏就變成選取文字、iOS 還會跳長按選單把拖曳中斷。
  改成整列都是把手**：`.expense-row.sorting-row` 上 `touch-action:none`（瀏覽器不要搶去捲動）、
  `user-select`／`-webkit-touch-callout` 關掉選字、`>*{pointer-events:none}` 讓子元素不吃事件
  （否則拖完會誤觸頭像、刪除鈕）
- 另外三道防線：拖曳期間 `body` 的 `userSelect`＋`webkitUserSelect` 關掉、攔 `selectstart`、
  開拖時清掉已經選起來的文字
- 代價：排序模式中手指在列上滑不會捲動頁面（要捲就從日期標題列滑，或拖到邊緣自動捲）
- 放開時只改「需要改」的那幾筆：順序本來就對的完全不寫資料庫；被拖的那筆和原本沒有排序值的，
  才用前後鄰居內插出一個值
- 前一版的 ▲▼ 按鈕已移除。它有個實際的 bug：舊帳目沒有值時比較會算出 `Infinity - Infinity = NaN`，
  排序結果不穩定（按了看起來沒動），而且整天都沒值時按一下會直接跳到當天第一位

⚠️ **舊帳目沒有 `time_min`**，一律不顯示時間，要的話進去自己填。
刻意不回填：現有 `sort_order` 有一部分是「掃描當下」的假時間，回填等於把錯的時間變成正式資料。

### 5-4 封存後可查看

原本 `openEditModal()` 第一行直接擋掉封存旅程，現在改成**開啟但唯讀**：
標題顯示「查看花費」、欄位禁用、儲存鈕隱藏、分攤勾選點了會提示、照片刪除鈕隱藏，
上方提示「旅程已封存，僅供查看」。用 `state.viewOnly` 旗標控制，沿用既有的「非發起人唯讀」機制。

### 5-5 日期收合（2026-08-30）

「按日期」分頁的每一天**預設收起來**，點標題那一列才展開，再點一次收回去。

**外觀**：收起來時每一天是一張卡（白底、`--border` 邊框、`--radius-sm`，跟 `.expense-row` 同一套語言），
左邊 SVG 箭頭（展開轉 90 度變橘色）、日期寫成 `08/30 週日`、當天多一個橘色「今天」標籤，
右邊上下兩行：主幣別合計（DM Mono）＋ `NT$xxx · N 筆`。
展開後標題退成透明無框、右側只留「合計 ¥x,xxx」與 ⇅ 排序，讓下面的明細當主角。
相關 CSS 在 `.date-header.collapsible`（`.open` 是展開狀態），`.date-group.folded` 負責收起時的間距。

| 情況 | 行為 |
|---|---|
| 搜尋中 | 全部展開（不然搜到的東西還藏在收起來的日期裡）|
| 正在排序某天 | 那天強制展開 |
| 剛存完一筆帳 | 自動展開那一天，不然存完看不到自己剛記的 |
| 重新整理 | 全部收回去（展開狀態只存在記憶體的 `expandedDays`，沒寫 localStorage）|

「分類」「付款人」兩個分頁**維持原樣不收合** —— 需求只提到「每一天」，沒動它們。
`grpHdr()` 的第四個參數 `open` 傳 `undefined` 就是不收合。

⚠️ 標題列整條可點，所以 **⇅ 排序** 按鈕加了 `event.stopPropagation()`，
不然按排序會順便把那天收起來。

---

## 六、其他已完成的修正

| 項目 | 說明 |
|---|---|
| **XSS** | 新增 `esc()`／`jsq()`，套用 41 處輸出點 |
| **分攤匯率失衡** | 自訂金額分攤改為依比例分配台幣總額，已用 node 實測跨匯率平衡 |
| **ID 碰撞** | 手動記帳改用 `uid()`（時間戳記+隨機碼）|
| **重複建立旅程** | 加防連點旗標，中途失敗會回收空旅程 |
| **日期時區** | `toISOString()` 是 UTC，凌晨會標成前一天 → 統一用 `todayStr()` |
| **備註數量顯示** | 原本「¥556 x2」會被誤讀成 1,112，改成「¥278×2 = ¥556」|
| **回憶錄分頁隱藏** | 底部導覽按鈕用 HTML 註解包起來，搜尋「回憶錄分頁暫時隱藏」可復原 |
| **封存移到設定頁** | 原本在回憶錄頁底部，該頁隱藏後移到設定 |
| **視窗照片可放大** | 編輯／查看視窗裡的照片點一下可全螢幕看，收據小字才看得清 |
| **彈窗防橫移** | `.modal` 加 `overflow-x:hidden`＋`overscroll-behavior-x:none`，新增紀錄時不會左右滑 |
| **編輯視窗右上叉叉** | 點進某一筆之後可以直接按 ✕ 關掉，不必捲到最底按「取消」|

---

## 七、已隱藏／未完成的功能

### 回憶錄（分頁已隱藏）

| 項目 | 說明 |
|---|---|
| **AI 生成叫不出來** | `generateMemoir()` 寫得很完整，但 `renderMemoir()` 畫出的畫面裡沒有生成按鈕、也沒有它要寫入的 `memoirBody` 容器。只有它自己內部的「重新生成／重試」在呼叫，形成死循環 |
| **模型已退場** | 該函式用 `claude-sonnet-4-20250514`，2026-06-15 下線，現在呼叫會 404 |
| **PDF 中文空白** | jsPDF 用內建 `helvetica`，沒載中文字型 |
| **Gemini key 沒 UI** | `state.googleApiKey` 只能從 localStorage 讀 |

其餘四顆匯出按鈕（海報／Email／HTML／AI 素材）功能正常，其中「匯出 AI 素材」不需金鑰。

---

## 八、待辦

### 已討論但未做（有明確方案）

| 項目 | 說明 |
|---|---|
| **移除效能量測碼** | 確認旅伴都更新到新版後就可拿掉 |
| **照片背景上傳** | 儲存時要等照片上傳完（實測 7~17 秒）。改成背景上傳可讓儲存秒完成，代價是傳完前關 App 會遺失照片 |
| **品項明細結構化** | 在 `expenses` 加 `items` 欄位存 JSON，才能事後重新編輯品項歸屬。需要 SQL 加欄位 + 一整套重編介面 |
| **商品名翻譯規則** | 品牌商品名（如「橋本 可愛いくろぼう」）沒有通用中文譯名時會原樣保留日文。可在提示詞加規則要求意譯或音譯＋品類。**要重新部署函式** |
| **重新部署 Edge Function** | 見 3-5、5-3。**已確定沒部署**：掃描讀不到收據上的時間。部署後新掃的收據才會有真實的 🕐，順便確認 `effort` 現值 |
| **回憶錄接回介面** | 分頁隱藏中。要用的話得補生成按鈕與容器，並把模型換成 `claude-sonnet-5` |

### 尚未處理

| 優先度 | 項目 | 說明 |
|---|---|---|
| 高 | **其他資料表未受保護** | 同一個 Supabase 專案還有 `job_postings`(2,485筆)、`clinic_signals`、`notification_log` 等其他專案的資料表，全部匿名可讀。**刻意沒動**——不知道那些 App 怎麼存取，貿然開 RLS 會讓它們全斷 |
| 中 | 照片是公開網址 | Supabase Storage 公開連結，有網址就看得到。要改簽名網址 |
| 中 | 舊帳目日期錯誤 | 先前凌晨記的帳日期是前一天，程式不會自動修正 |
| 低 | 收據存檔的 ID 寫法 | `saveReceiptExpenses()` 仍用 `'exp_'+(now+idx)`，沒跟著改用 `uid()`。同毫秒存兩張收據理論上會撞號 |
| 低 | `device_id` 欄位是死的 | `members` 表有欄位但程式沒寫入 |
| 低 | 免註冊升級 UI | `upgradeGuestAccount()` 已寫好但沒接介面 |
| 選用 | 三個無主旅程 | test(7筆)、日本(1筆)、123(1筆)，`supabase_claim_trips.sql` 可掛到擁有者帳號 |

---

## 九、這個專案的坑

1. **`toISOString()` 不能用** —— 那是 UTC。要本地日期一律用 `todayStr()`。
2. **註解裡不能出現字面的 `</script>`** —— 會提前終止程式區塊，後面所有 JS 被當 HTML 解析。曾造成線上全斷。
3. **語法檢查要檢查標籤數量配對 + 逐區塊驗證** —— 用 `<script>(.*?)</script>` 非貪婪比對會在第一個結束標籤就停住，截斷後的片段可能剛好語法正確而漏掉問題。
4. **RLS 政策是 OR 關係** —— 新增嚴格政策不會覆蓋既有的寬鬆政策，必須先 `drop policy`。忘了 drop 會收到 `42710: policy already exists`，該語句不生效、資料庫維持原狀。
5. **改 RLS 要連程式一起上線** —— 政策與程式的存取順序必須相符（`createTrip` 必須先寫 `user_trips` 才能寫 `members`）。
6. **金鑰有三把**：`sb_publishable_...`（在 index.html，公開沒關係）、`sb_secret_...`（管理工具用，絕不可外流）、`ANTHROPIC_API_KEY`（在 Supabase Secrets）。
7. **升級 Claude 模型要重看參數預設值** —— 同段程式在不同世代行為不同。實例：不帶 `thinking` 時 `sonnet-4-6` 不思考、`sonnet-5` 會自動思考，而思考與輸出共用 `max_tokens`，可能導致 JSON 被截斷。
8. **sonnet-5 對中日文較耗 token** —— 新斷詞器讓同一份中日文提示詞多算約 30% token，實測每張收據從 $0.02 變 $0.03。
9. **有些改動不是跑 `發布上線.bat` 就好** —— 提示詞與模型參數在 Edge Function 裡，要到 Supabase Dashboard 重新部署；加欄位要跑 SQL。三者順序：SQL → 函式 → 前端。
10. **改了要確認真的上線** —— 這次除錯有兩次是在「還沒發布的舊版」上測，繞了冤枉路。改完後用 curl 抓線上檔案 grep 關鍵字確認，比問「你發布了嗎」可靠。
11. **不要用推測填補缺少的資訊** —— 這次把「掃描失敗」歸因於模型、把「變慢」歸因於網路環境，都是猜的，最後真正原因是照片超過 10MB（錯誤訊息一貼就破案）。先拿錯誤訊息和數字，再下判斷。

---

## 十、常用查核指令

```bash
# 線上版本是否含某項改動
curl -s "https://pzm50160.github.io/japan-travel-tracker/?v=$(date +%s)" | grep -c "關鍵字"

# 匿名登入開關（唯讀，不需登入）
curl -s "https://lkojnzprqqdmqhzzlqtb.supabase.co/auth/v1/settings" -H "apikey: <publishable key>"

# Edge Function 是否存活（預期回 401「請先登入再使用掃描」）
curl -s -X POST "https://lkojnzprqqdmqhzzlqtb.supabase.co/functions/v1/scan-receipt" \
  -H "apikey: <publishable key>" -H "Content-Type: application/json" -d '{}'
```

```bash
# 某個欄位是否已建立（回 [] 表示有；回 42703 表示沒有）
curl -s "https://lkojnzprqqdmqhzzlqtb.supabase.co/rest/v1/expenses?select=sort_order&limit=1"   -H "apikey: <publishable key>" -H "Authorization: Bearer <publishable key>"
```

```sql
-- 某張表目前的政策
select policyname, cmd, qual, with_check from pg_policies
where schemaname='public' and tablename='trips' order by policyname;
```

---

## 十一、檔案清單

```
D:\dev\日本記帳\
  index.html                              主程式
  packing-list.html                       （獨立的行李清單頁）
  發布上線.bat                            一鍵發布
  supabase/functions/scan-receipt/index.ts  收據辨識代理（需在 Dashboard 部署）
  supabase_rls.sql                        RLS 政策（已執行）
  supabase_functions.sql                  三個邀請碼函式（已執行）
  supabase_guest.sql                      免註冊權限規則（已執行）
  supabase_guest_no_create.sql            免註冊不得建旅程（已執行）
  supabase_sort_order.sql                 同一天內排序欄位（已執行）
  supabase_time_field.sql                 真實時間 time_min／手動排序 sort_manual（⚠️ 待執行，要先於前端發布）
  supabase_claim_trips.sql                舊旅程掛載（選用，未執行）
  交接說明.md                              本檔
  旅遊記帳_使用說明.pdf / _快速上手.pdf
  邀請碼備份.txt 等三個備份檔（已被 .gitignore 排除）

D:\dev\旅遊記帳-管理工具\
  旅程管理.pyw                            視窗版管理工具
  service_key.txt                         Secret key（已排除於版控）
```

## 使用者對應

| 帳號 | 對應 | 備註 |
|---|---|---|
| `r5500160@gmail.com` | 專案擁有者本人（成員名稱「名」）| 阿里山、南新名古屋音樂交流、日本 |
| `tooya47@yahoo.com.tw` | 小鳳 | 九州之旅（實際使用中）|
| `ily64247@gmail.com` | 首辰？ | 南新名古屋音樂交流 |
| 其餘 3 個 | 未確認 | 對應旅程都是 0 筆花費的舊測試 |
