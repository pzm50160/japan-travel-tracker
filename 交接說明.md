# 旅遊記帳 — 專案交接說明

> 更新於 2026-08-18（第二版）。新對話請先讀這份，可略過先前的討論過程。

## 專案基本資料

| 項目 | 內容 |
|---|---|
| 程式 | 單一檔案 `index.html`（約 5,000 行，HTML+CSS+JS 全在一起）|
| 線上網址 | https://pzm50160.github.io/japan-travel-tracker/ |
| 部署方式 | GitHub Pages，推上 main 分支約 30 秒後自動生效 |
| 發布指令 | 雙擊 `發布上線.bat`（git add → commit → push）|
| 資料庫 | Supabase 專案 `lkojnzprqqdmqhzzlqtb`（免費方案）|
| 後台 | https://supabase.com/dashboard/project/lkojnzprqqdmqhzzlqtb |
| 使用者規模 | 18 個旅程、21 位成員、38 筆花費、6 個註冊帳號 |

功能分頁：首頁、紀錄、掃描（AI 讀日文收據）、分攤、統計、行李、牌局計分、設定。
（回憶錄分頁已隱藏，見下方）

---

## 目前狀態總覽

| 項目 | 狀態 |
|---|---|
| RLS 資安修補 | ✅ 已完成並驗證 |
| 三支 SQL（rls / functions / guest） | ✅ 已執行 |
| 匿名登入開關 | ✅ 已開啟（實測 `anonymous_users: true`）|
| 免註冊不得建立旅程 | ✅ 已執行 `supabase_guest_no_create.sql` |
| 程式改動 | ✅ 已發布（commit `3d33a77`）|
| **免註冊完整流程實測** | ⚠️ **尚未有人走過，需要實測** |
| **掃描新參數實測** | ⚠️ **尚未實測，需掃一張多品項收據確認** |

---

## 已完成的工作

### 1. 資安漏洞修補（已完成並驗證）

**問題**：`trips`／`members`／`expenses` 三張表各有一條政策
`public read write`（對象 `{public}`、條件 `true`、動作 `ALL`），
任何人拿公開金鑰（就寫在 index.html 裡、公開在 GitHub）都能
**讀取、修改、刪除全部資料**。另外 `user_trips` 的政策未限制 `trip_id`，
登入者可自行插入紀錄取得任意旅程存取權。

**修法**：移除那四條政策，改成「必須是該旅程成員」，
並新增三個 `SECURITY DEFINER` 函式讓邀請碼流程在鎖上後仍能運作。

**驗證**（匿名金鑰實測）：

| 測試 | 修補前 | 修補後 |
|---|---|---|
| 讀取 trips / members / expenses | 18 / 21 / 38 筆 | 全部 0 筆 |
| 新增旅程 | 成功 | HTTP 401 |
| 呼叫邀請碼函式 | — | 42501 permission denied |

相關檔案：`supabase_rls.sql`（政策）、`supabase_functions.sql`（三個函式）。

### 2. 程式 bug 修正（已上線）

| 項目 | 說明 |
|---|---|
| **XSS** | 新增 `esc()`／`jsq()`，套用 41 處輸出點。旅伴原本可用消費項目名稱在他人手機執行程式碼 |
| **分攤匯率失衡** | 自訂金額分攤原本用「當前匯率」重算，但金額本身凍結在記帳當下 → 改匯率後帳目對不起來且畫面看不出來。改為依比例分配台幣總額，已用 node 實測跨匯率仍平衡 |
| **ID 碰撞** | `'exp_'+Date.now()` 同毫秒會覆蓋 → 改用 `uid()`（時間戳記+隨機碼，仍可還原建立時間）|
| **重複建立旅程** | 加防連點旗標，中途失敗會回收空旅程（先前有人 11 分鐘內建了 6 個「道東14天之旅」）|
| **日期時區** | `toISOString()` 是 UTC，台灣凌晨 0~8 點會標成前一天，且該筆不會出現在首頁「今日花費」→ 統一為 `todayStr()` |
| **舊複本下線** | 刪除 `japan-travel-tracker.html`（index.html 的過期複本，仍線上可存取）|

### 3. 免註冊模式（已完成並啟用）

原本的「訪客模式」沒有任何身分，資料庫無從判斷誰能看哪個旅程 ——
這正是漏洞的根源之一。改用 **Supabase 匿名登入**：使用者不用填任何資料，
但系統會發給一個 `auth.uid()`，RLS 才能運作。

**權限設計**：免註冊者可以「參與」——加入旅程、記帳、看分攤，
但不能「破壞」——建立旅程、改旅程設定、移除其他成員。

`supabase_guest.sql` 建立 `is_anon_user()` 函式並限制 trips 的 UPDATE、members 的 DELETE。
`supabase_guest_no_create.sql` 另外限制 trips 的 INSERT（見下）。

### 4. 免註冊不得建立旅程（2026-08-18 新增）

**問題**：介面上那顆按鈕寫的是「免註冊，用邀請碼加入」，
但 `trips_insert` 政策只檢查 `auth.uid() is not null`，匿名身分同樣通過；
程式端 `renderMyTrips()` 也只判斷 `state.user` 有沒有值，
匿名登入後 `state.user` 是有值的，所以整支走的是正式帳號那條路，
免註冊者看到的畫面跟註冊帳號一模一樣，什麼都能做。

匿名身分綁在該裝置的瀏覽器，清除資料就找不回來，
它建立的旅程會變成**無主旅程**——專案裡現存的三個無主旅程，成因很可能就是這個。

**修法**：

| 層級 | 改動 |
|---|---|
| 資料庫 | `supabase_guest_no_create.sql`：`trips_insert` 加上 `and not public.is_anon_user()`（已執行並驗證）|
| 程式 | 新增 `isGuestUser()`（判斷 `state.user.is_anonymous`）|
| 程式 | `renderMyTrips()`：免註冊時隱藏「建立新旅程」按鈕、標籤顯示「免註冊」（原本顯示空白 email）|
| 程式 | `showCreateTrip()` 加防呆，繞過按鈕也擋得住 |
| 文案 | 訪客橫幅原本寫「旅程只存在這台裝置」，改用匿名登入後已不正確（資料在雲端），改為說明「此身分只存在這台裝置」|

### 5. 回憶錄分頁隱藏、封存移到設定（2026-08-18）

回憶錄分頁目前用不到，底部導覽的按鈕已用 HTML 註解包起來
（`index.html` 搜尋「回憶錄分頁暫時隱藏」，拿掉註解即可復原）。
頁面本體與所有匯出函式都原樣保留。

原本置於回憶錄頁底部的「封存旅程／解除封存」已移到**設定頁**
（設定頁新增 `<div id="archiveCard">`，由 `renderSettings()` 填入）。
`archiveTrip()` / `unarchiveTrip()` 成功後改呼叫 `renderSettings()` 重繪。

### 6. 收據掃描優化（2026-08-18）

| 項目 | 說明 |
|---|---|
| **關閉思考** | 加上 `thinking:{type:'disabled'}`。**這是還原，不是新增限制**——原本用 4-6 時不帶此參數等於不思考；升級 sonnet-5 後同樣的程式碼會**預設開啟思考**，而思考與輸出共用 `max_tokens:3000`，品項多的收據會在 JSON 寫到一半被切斷，`JSON.parse` 直接失敗 |
| **結構化輸出** | 加上 `output_config.format` + `RECEIPT_SCHEMA`，格式由 API 強制保證。順手刪掉 `.replace(/```json\|```/g,'')` 那段硬扒 markdown 的程式碼。schema 的分類與付款方式 enum 直接引用既有的 `CATS`／`PAYS` 常數，不另外寫死 |
| **截斷防護** | 加 `stop_reason==='max_tokens'` 檢查，萬一仍截斷會顯示「收據品項太多」而不是丟一個看不懂的 JSON 錯誤 |
| **模型** | `claude-sonnet-4-6` → `claude-sonnet-5`（解析度上限 1568→2576px）|

**刻意沒做**：提示詞快取。要快取那份很長的日文對照表，必須把文字挪到圖片前面（快取只認前綴，圖片每次都不同），但實際只省下每張約一美分，卻要動到收據辨識的輸入順序、有影響準確度的風險。不划算。

### 7. 其他

- 說明書兩份 PDF（`旅遊記帳_使用說明.pdf` 6 頁、`旅遊記帳_快速上手.pdf` 1 頁），
  產生腳本在暫存區 `make_manual.py` / `make_quick.py`
- 管理工具在 `D:\dev\旅遊記帳-管理工具\旅程管理.pyw`（tkinter 視窗程式，
  可看所有旅程／成員／擁有者，刪除前強制備份）
- `.gitignore` 排除含邀請碼的備份檔

---

## 待辦事項

### 需要實測（最優先，都還沒有人走過）

1. **免註冊完整流程**：無痕視窗 → 免註冊 → 輸入邀請碼加入 → 確認四件事：
   看得到帳目、能新增花費、**看不到「建立新旅程」**、設定頁改不動旅程名稱
2. **掃描新參數**：掃一張**多品項的超市收據**，確認辨識正常
   （改了 `thinking` 與 `output_config` 兩個參數，尚未實機驗證）

### 回憶錄相關（分頁已隱藏，不急）

| 項目 | 說明 |
|---|---|
| **AI 生成叫不出來** | `generateMemoir()` 寫得很完整，但 `renderMemoir()` 畫出來的畫面裡**沒有生成按鈕，也沒有它要寫入的 `memoirBody` 容器**。全檔只有它自己內部的「重新生成／重試」在呼叫——那些要先成功執行一次才會出現，形成死循環。所以 `trips.memoir_text` 對一般使用者永遠是空的 |
| **模型已退場** | 該函式用 `claude-sonnet-4-20250514`，此 ID 於 2026-06-15 下線，現在呼叫會 404。接回介面時要換成 `claude-sonnet-5` |
| **PDF 中文空白** | 匯出 PDF 用 jsPDF 內建 `helvetica`，沒載中文字型，旅程名／分類／內文都印不出來。要嘛外掛字型檔（單檔會胖 2~9MB），要嘛拿掉這顆按鈕改用 HTML 匯出＋瀏覽器列印 |
| **Gemini key 沒輸入框** | `state.googleApiKey` 只能從 localStorage 讀，設定頁沒地方填，等於死的 |

其餘四顆匯出按鈕（海報 / Email / HTML / AI 素材）功能正常。
其中「匯出 AI 素材」不需要 API Key，是目前最實用的一顆。

### 尚未處理

| 優先度 | 項目 | 說明 |
|---|---|---|
| 高 | **其他資料表未受保護** | 同一個 Supabase 專案還有 `job_postings`(2,485 筆)、`clinic_signals`(12)、`notification_log`(12) 等其他專案的資料表，全部匿名可讀。**刻意沒動** —— 不知道那些 App 怎麼存取，貿然開 RLS 會讓它們全斷 |
| 中 | API Key 存在瀏覽器 | `state.apiKey` 放 localStorage，用 `anthropic-dangerous-direct-browser-access` 直接從前端呼叫 Anthropic。拿到這台手機的人、或任何 XSS 破口都能撈走去刷帳。正解是後端代理（Supabase Edge Function），Key 放伺服器 |
| 中 | 照片是公開網址 | Supabase Storage 公開連結，有網址就看得到。要修需改用簽名網址 |
| 中 | 舊帳目日期錯誤 | 先前凌晨記的帳日期是前一天，程式不會自動修正，需手動改 |
| 中 | 收據照片未縮圖 | 手機原圖常 3000px 以上，sonnet-5 單張最多吃 ~4784 image token（舊上限約 1568）。收據文字 1500px 就夠清楚，前端 canvas 縮圖可省錢也上傳更快 |
| 低 | `device_id` 欄位是死的 | `members` 表有這欄位但程式從沒寫入 |
| 低 | 免註冊升級 UI | `upgradeGuestAccount()` 函式已寫好但沒接介面 |
| 選用 | 三個無主旅程 | test(7筆)、日本(1筆)、123(1筆)，`supabase_claim_trips.sql` 可掛到擁有者帳號，不做也不會壞 |

---

## 重要資訊

### 使用者對應

| 帳號 | 對應 | 備註 |
|---|---|---|
| `r5500160@gmail.com` | 專案擁有者本人（成員名稱「名」）| 阿里山、南新名古屋音樂交流、日本 |
| `tooya47@yahoo.com.tw` | 小鳳 | 九州之旅（8/18~8/27，實際使用中）|
| `ily64247@gmail.com` | 首辰？ | 南新名古屋音樂交流 |
| 其餘 3 個 | 未確認 | 對應的旅程都是 0 筆花費的舊測試 |

### 這個專案的坑

1. **`toISOString()` 不能用** —— 那是 UTC。要本地日期一律用 `todayStr()`。
2. **註解裡不能出現字面的 `</script>`** —— 會提前終止程式區塊，
   後面所有 JS 被當 HTML 解析。曾因此造成線上全斷。
3. **語法檢查要檢查標籤數量配對 + 逐區塊驗證** —— 單純用
   `<script>(.*?)</script>` 非貪婪比對抽程式碼，會在第一個結束標籤就停住，
   截斷後的片段可能剛好語法正確而漏掉問題。
4. **RLS 政策是 OR 關係** —— 新增嚴格政策不會覆蓋既有的寬鬆政策，
   必須先 `drop policy`。（改政策時忘了 drop 會收到
   `42710: policy already exists`，該語句不會生效，資料庫維持原狀。）
5. **改 RLS 要連程式一起上線** —— 政策與程式的存取順序必須相符，
   否則 App 會壞（`createTrip` 必須先寫 `user_trips` 才能寫 `members`）。
6. **金鑰有兩把**：`sb_publishable_...`（放在 index.html，公開沒關係，
   前提是 RLS 有設好）、`sb_secret_...`（管理工具用，絕不可外流）。
7. **升級 Claude 模型時要重看參數預設值** —— 同一段程式碼在不同世代行為不同。
   實例：不帶 `thinking` 參數時，`claude-sonnet-4-6` 是不思考，
   `claude-sonnet-5` 卻會**自動思考**，而思考與輸出共用 `max_tokens`。
   換模型不是只換字串，要順便查該版的預設行為與已移除的參數。

### 檔案清單

```
D:\dev\日本記帳\
  index.html                    主程式
  packing-list.html             （獨立的行李清單頁）
  發布上線.bat                  一鍵發布
  supabase_rls.sql              RLS 政策（已執行）
  supabase_functions.sql        三個邀請碼函式（已執行）
  supabase_guest.sql            免註冊權限規則（已執行）
  supabase_guest_no_create.sql  免註冊不得建立旅程（已執行）
  supabase_claim_trips.sql      舊旅程掛載（選用，未執行）
  交接說明.md                    本檔
  旅遊記帳_使用說明.pdf / _快速上手.pdf
  邀請碼備份.txt 等三個備份檔（已被 .gitignore 排除）

D:\dev\旅遊記帳-管理工具\
  旅程管理.pyw                  視窗版管理工具
  service_key.txt               Secret key（已排除於版控）
```

### 查核指令備忘

```bash
# 匿名登入開關是否開啟（不需登入，唯讀）
curl -s "https://<專案>.supabase.co/auth/v1/settings" -H "apikey: <publishable key>"

# 某個政策目前的條件
select policyname, cmd, qual, with_check from pg_policies
where schemaname='public' and tablename='trips' order by policyname;
```
